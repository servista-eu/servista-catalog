---
phase: 02-verify-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]

must_haves:
  truths:
    - "Version catalog is the sole published artifact (no other variants)"
    - "Catalog contains exactly 35 versions, 75 library aliases, and 6 plugin aliases"
    - "Catalog contains 9 qabatz-kotlin-commons library entries"
    - "Catalog contains 1 qabatz-kotlin-ktor library entry"
    - "REQUIREMENTS.md reflects the actual catalog counts (35 versions, 75 libraries)"
    - "PROJECT.md reflects the actual catalog counts (35 versions, 75 libraries)"
  artifacts:
    - path: "catalog/libs.versions.toml"
      provides: "Published version catalog with all dependency entries"
      contains: "qabatz-commons-core"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement CAT-02 with correct counts"
      contains: "35 versions, 75 libraries"
    - path: ".planning/PROJECT.md"
      provides: "Updated validated requirements with correct counts"
      contains: "35 versions, 75 library aliases"
  key_links:
    - from: "build.gradle.kts"
      to: "catalog/libs.versions.toml"
      via: "from(files()) in catalog block"
      pattern: "from\\(files\\(\"catalog/libs\\.versions\\.toml\"\\)\\)"
---

<objective>
Verify the version catalog is complete and correct, then update documentation with accurate counts.

Purpose: Formally confirm all Phase 2 requirements (CAT-01 through CAT-04) using automated verification, and correct the inflated library count (~140) in planning documents to the actual count (75). This closes the verification phase and establishes the catalog baseline for publishing in Phase 3.

Output: Verified catalog with documented baseline counts in REQUIREMENTS.md and PROJECT.md.
</objective>

<execution_context>
@$HOME/.claude-bc/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-bc/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-verify-catalog/02-CONTEXT.md
@.planning/phases/02-verify-catalog/02-RESEARCH.md

@catalog/libs.versions.toml
@build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify catalog integrity and completeness</name>
  <files>catalog/libs.versions.toml</files>
  <read_first>
    - catalog/libs.versions.toml (the published catalog to verify)
    - build.gradle.kts (confirms catalog-only build configuration)
    - .planning/phases/02-verify-catalog/02-RESEARCH.md (contains expected counts and verification commands)
  </read_first>
  <action>
Run all four verification checks to formally confirm CAT-01 through CAT-04. Per D-03, use both TOML audit and Gradle tasks.

**CAT-01 — Sole artifact verification:**
Run `./gradlew outgoingVariants 2>&1 | grep -c "Variant "` and confirm the result is exactly `1`. The single variant must be `versionCatalogElements` with category `platform` and usage `version-catalog`.

**CAT-02 — Entry count verification:**
Run these awk commands against `catalog/libs.versions.toml`:
```
awk '/^\[versions\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l
```
Expected: 35 versions.
```
awk '/^\[libraries\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l
```
Expected: 75 libraries.
```
awk '/^\[plugins\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l
```
Expected: 6 plugins.

**CAT-03 — Qabatz Commons entries:**
Run `grep -c "qabatz-commons" catalog/libs.versions.toml` and confirm the result is `9`. The 9 entries are: core, adapter-kafka, adapter-valkey, adapter-jooq, adapter-otel, adapter-micrometer, adapter-konform, kafka-avro, secrets.

**CAT-04 — Qabatz Ktor entries:**
Run `grep -c "qabatz-ktor" catalog/libs.versions.toml` and confirm the result is `1` (the `qabatz-ktor` library alias mapping to `eu.qabatz:qabatz-kotlin-ktor`).

**Gradle parse verification (D-03):**
Run `./gradlew generateCatalogAsToml` and confirm BUILD SUCCESSFUL. This validates that Gradle can parse all TOML syntax, resolve all version.ref values, and produce a normalized catalog output.

**Structural integrity (D-04, D-05):**
Check for orphaned version refs by running:
```
awk '/^\[versions\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | sed 's/ *=.*//' | while read ver; do if ! grep -q "version.ref = \"$ver\"" catalog/libs.versions.toml; then echo "ORPHANED: $ver"; fi; done
```
Expected: no output (no orphaned versions).

Per D-04, if ANY check fails, fix the issue in catalog/libs.versions.toml before proceeding. Per D-05, verify naming consistency within sections (the research found all naming divergences are intentional per Gradle best practices -- confirm this holds).

Per Claude's discretion on bundles: Do NOT add bundle definitions. The research recommends against it and this is a verification phase, not a feature-addition phase.

No file modifications expected in this task unless a verification check fails (D-04 requires fixing issues found).
  </action>
  <verify>
    <automated>cd /home/sven/work/clients/hestia/projects/qabatz/libs/qabatz-gradle-plugins && ./gradlew generateCatalogAsToml 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew outgoingVariants 2>&1 | grep -c "Variant "` outputs `1`
    - `awk '/^\[versions\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l` outputs `35`
    - `awk '/^\[libraries\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l` outputs `75`
    - `awk '/^\[plugins\]/{p=1;next} /^\[/{p=0} p && /^[a-zA-Z]/' catalog/libs.versions.toml | wc -l` outputs `6`
    - `grep -c "qabatz-commons" catalog/libs.versions.toml` outputs `9`
    - `grep -c "qabatz-ktor" catalog/libs.versions.toml` outputs `1`
    - `./gradlew generateCatalogAsToml` outputs BUILD SUCCESSFUL
    - Orphaned version ref check produces no output (no orphaned refs)
  </acceptance_criteria>
  <done>All four requirements (CAT-01 through CAT-04) verified via automated commands. Catalog confirmed as sole artifact with 35 versions, 75 libraries, 6 plugins, 9 commons entries, and 1 ktor entry. No structural issues found (or all issues fixed per D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation with verified catalog counts</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (contains inflated count "~40 versions, ~140 libraries" on line 14)
    - .planning/PROJECT.md (contains inflated count "~40 versions, ~140 library aliases" on line 15)
  </read_first>
  <action>
Per D-02, update both documentation files with the precise verified counts from Task 1.

**REQUIREMENTS.md** (line 14):
Change:
```
- [ ] **CAT-02**: Catalog includes all current dependency versions (~40 versions, ~140 libraries, 6 plugins)
```
To:
```
- [ ] **CAT-02**: Catalog includes all current dependency versions (35 versions, 75 libraries, 6 plugins)
```

**PROJECT.md** (line 15, in the "Validated" section under "Requirements"):
Change:
```
- ✓ Published version catalog with ~40 versions, ~140 library aliases, 6 plugin aliases — existing
```
To:
```
- ✓ Published version catalog with 35 versions, 75 library aliases, 6 plugin aliases — existing
```

Also update ROADMAP.md Phase 2 success criteria (line 47):
Change:
```
  2. libs.versions.toml contains all ~40 versions, ~140 library aliases, and 6 plugin aliases
```
To:
```
  2. libs.versions.toml contains all 35 versions, 75 library aliases, and 6 plugin aliases
```

These are the ONLY changes. Do not modify any other content in these files.
  </action>
  <verify>
    <automated>cd /home/sven/work/clients/hestia/projects/qabatz/libs/qabatz-gradle-plugins && grep -c "~140" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md 2>/dev/null; echo "---"; grep "CAT-02" .planning/REQUIREMENTS.md; echo "---"; grep "35 versions" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "~140" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` produces no output (no matches)
    - `grep -r "~40 versions" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` produces no output (no matches)
    - .planning/REQUIREMENTS.md line containing CAT-02 includes the string "35 versions, 75 libraries, 6 plugins"
    - .planning/PROJECT.md contains the string "35 versions, 75 library aliases, 6 plugin aliases"
    - .planning/ROADMAP.md Phase 2 success criteria contains "35 versions, 75 library aliases"
  </acceptance_criteria>
  <done>All three planning documents updated with precise verified counts (35 versions, 75 libraries, 6 plugins). No stale ~140 or ~40 references remain in any planning file.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full verification suite:

1. `./gradlew generateCatalogAsToml outgoingVariants` -- both tasks succeed
2. `grep -r "~140\|~40 versions" .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` -- no matches
3. All awk count commands return expected values (35, 75, 6)
4. `grep -c "qabatz-commons" catalog/libs.versions.toml` returns 9
5. `grep -c "qabatz-ktor" catalog/libs.versions.toml` returns 1
</verification>

<success_criteria>
- CAT-01: `outgoingVariants` shows exactly 1 variant (versionCatalogElements)
- CAT-02: Catalog contains 35 versions, 75 libraries, 6 plugins; documentation updated to match
- CAT-03: Catalog contains 9 qabatz-kotlin-commons entries (grep confirms)
- CAT-04: Catalog contains 1 qabatz-kotlin-ktor entry (grep confirms)
- No stale count references (~140, ~40) remain in planning documents
- `generateCatalogAsToml` succeeds (Gradle can parse the catalog)
</success_criteria>

<output>
After completion, create `.planning/phases/02-verify-catalog/02-01-SUMMARY.md`
</output>
